params {
    samplesheet  = "templates/samplesheet.csv"
    outdir = "results"
}

process {
    cpus   = 1
    memory = '4.GB'
    time   = '1.h'
}

profiles {
    docker {
        process.executor = 'local'
	docker.enabled = true
	docker.runOptions = '--user $(id -u):$(id -g)' 
    }
}

